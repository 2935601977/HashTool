# 更新日志

本项目的版本变更都记在这里，格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

### 修复
- **把文件 / 文件夹拖到 exe 图标上不生效**：Windows 是通过「启动参数」把路径传给程序的，
  而之前只认得 `--selftest` 这类开关，路径参数被直接丢掉，所以只会弹出一个空窗口。
  现在启动时会把命令行里的文件 / 文件夹加进列表并自动开始计算。
  （`--selftest` 里加了参数解析断言，`--uitest` 的首次加载也改成走这条真实启动路径。）
- **高缩放屏幕上界面被裁**：WinForms 的字体自动缩放会把布局按比例缩小（实测缩到 0.84），
  而文字是按真实 DPI 画的，导致下拉框文字显示不全、控件上下被切。
  现在关掉框架的自动缩放，改由程序按屏幕 DPI 计算行高/间距/窗口尺寸，
  下拉框宽度和列表列宽改成**按内容实测**（125% / 150% 缩放下都不会再裁字）。
- **右键菜单**：以前不管有没有选中行都显示同一套菜单。现在没选中时
  「复制 MD5/SHA-256」「复制路径/文件名/TSV/JSON」「重新计算选中」「移除选中」
  「在资源管理器中打开」会灰掉；选中的行还没算出哈希时，「复制 MD5/SHA-256」也不给点。
- **详情区在只算一个算法时**：没算的那个哈希框不再显示空值，而是明确写
  「（未计算：当前算法只算另一个）」并把字段名和「复制」按钮一起灰掉。
- 下拉框里「仅 MD5（更快）」改成「仅 MD5」（原来的括号说明太长，窄屏放不下）。

### 新增
- 自检增加「**界面布局无裁剪**」断言：自动检查各个下拉框宽度够不够、
  每一行的控件有没有超出可用高度（这就是上面那个高缩放问题的发现方式）。

### 变更
- **版本号收敛成单一定义**：只改 `src/HashTool.cs` 里的 `Const.Version` 一处，
  窗口标题、`--version`、导出的 JSON、程序集版本（自动补成 4 段的 `1.1.0.0`）
  和产品版本（`1.1.0`，与 git tag 一致）全部自动跟随。
- Release 流程新增校验：**tag 和代码里的 `Const.Version` 不一致时直接拒绝发布**。
- 删除英文 README，只保留中文主页。

## [1.1.0] - 2026-09-18

### 新增
- **F5 增量刷新**：用「文件大小 + 最后修改时间」当指纹，F5 时只重算变过的文件，
  没变的直接沿用上次结果；不想要这个行为可以取消勾选「F5 只重算变化的文件」。
- **计算算法可选**：`MD5 + SHA-256`（默认）/ `仅 MD5（更快）` / `仅 SHA-256`；
  切换时会自动把缺少的那个哈希补算上，导出的 JSON 里 `algorithms` 字段会记录当前算法。
- **Ctrl+C 复制内容可选**：`仅 MD5（默认）`/`仅 SHA-256`/`MD5 + SHA-256`/`文件名`/`完整路径`/`整行（TSV）`；
  选两个哈希时每个文件一行，行内用制表符分隔。
- 程序图标（蓝底白色大写 H，16–256 共 7 个尺寸）与作者信息。
- 命令行的两个自检入口：`--selftest`、`--uitest`、`--screenshot`。

### 修复
- 详情区左侧字段名在高 DPI 下会被挤成两行（"SHA-256" 折行），改为自适应宽度并加了自动断言。
- 高 DPI / 小屏幕上窗口可能超出屏幕，现在启动时会自动收缩到工作区内。

### 变更
- 状态栏在刷新后会说明「新增 / 变化 / 移除 / 未变化 / 本次重算」各多少个。
- 重构了工具栏布局（三行），避免窄窗口下控件被裁掉。

## [1.0.0] - 2026-09-18

### 新增
- 第一个可用版本：拖入文件 / 文件夹（递归可关）、同时计算 MD5 与 SHA-256、
  多线程并发（线程数可选）、进度条与取消、导出 JSON、右键复制（MD5 / SHA-256 / 路径 /
  文件名 / TSV / JSON）、重新计算全部或仅选中、大小写切换、F5 刷新、
  表头排序、关键词筛选、重复文件（同哈希）高亮、双击复制 SHA-256、Delete 移除、
  Ctrl+A 全选、被占用文件的容错（`FileShare.ReadWrite | Delete`）。
